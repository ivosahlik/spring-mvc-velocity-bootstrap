#macro( tablerows $color $somelist )
    #foreach( $something in $somelist )
        <tr><td bgcolor=$color>$something</td></tr>
    #end
#end

#macro( tablerowsall $color $somelist )
    #foreach( $something in $somelist )
        <tr>
            <td bgcolor=$color>$something.name &nbsp;</td>
            <td bgcolor=$color>$something.surname</td>
        </tr>
    #end
#end